@model IEnumerable<Adm_AutoGestion.Models.SummaryReport>
@{
    ViewBag.Title = "Dashboard";
    Layout = "~/Views/Shared/_LayoutMaestra.cshtml";
}
<link href="~/Contents/css/dashboard-style.css" rel="stylesheet" />
<style>
    .nav-underline .nav-link {
        border-bottom: 2px solid transparent;
    }

        .nav-underline .nav-link.active {
            border-bottom-color: currentColor;
        }
</style>

<h2 class="colorTitulo text-center">Reportes Acciones Ejecutadas</h2>

<div class="row m-0 bg-white" style="border-radius:3px;">

    <div class="row mt-3">
        <div class="col-lg-12 col-12">
            <div class="card shadow mb-4">
                <div class="card-header py-3">
                    <h6 class="m-0 font-weight-bold text-primary">
                        PROPORCIÓN ACCIONES DE FORMACIÓN EJECUTADAS DEL PROGRAMA DE
                        EDUCACIÓN CONTINUA
                    </h6>
                    <ul class="nav nav-underline">
                        <li class="nav-item px-2">
                            <a class="nav-link active" data-bs-toggle="tab" href="#General_Trimestre">Trimestre</a>
                        </li>
                        <li class="nav-item px-2">
                            <a class="nav-link" data-bs-toggle="tab" href="#General_Anual">Anual</a>
                        </li>
                    </ul>
                </div>
                <div class="tab-content">
                    <div id="General_Trimestre" class="card-body tab-pane fade show active">
                        <div class="mb-2" id="Indicador_Acciones_Ejecutadas_General"></div>
                    </div>
                    <div id="General_Anual" class="card-body tab-pane fade">
                        <div class="mb-2" id="Indicador_Acciones_Ejecutadas_General_Anual"></div>
                    </div>
                </div>

            </div>
        </div>
    </div>

    <div class="row mt-3">
        <div class="col-lg-12 col-12">
            <div class="card shadow mb-4">
                <div class="card-header py-3">
                    <h6 class="m-0 font-weight-bold text-primary">
                        PROPORCIÓN ACCIONES DE FORMACIÓN EJECUTADAS DEL PROGRAMA DE
                        EDUCACIÓN CONTINUA POR AREA
                    </h6>
                    <ul class="nav nav-underline">
                        <li class="nav-item px-2">
                            <a class="nav-link active" data-bs-toggle="tab" href="#Area_Trimestre">Trimestre</a>
                        </li>
                        <li class="nav-item px-2">
                            <a class="nav-link" data-bs-toggle="tab" href="#Area_Anual">Anual</a>
                        </li>
                        <li class="nav-item px-2">
                            <a class="nav-link" data-bs-toggle="tab" href="#Areas_Trimestre">General - Trimestre</a>
                        </li>
                        <li class="nav-item px-2">
                            <a class="nav-link" data-bs-toggle="tab" href="#Areas_Anual">General - Anual</a>
                        </li>
                    </ul>
                </div>
                <div class="fondoDegradado text-center text-white tablaheader">FOSCAL</div>

                <div class="tab-content">

                    <div id="Area_Trimestre" class="card-body mt-3 tab-pane fade show active">
                        <select class="selectpicker" id="Indicador_Acciones_Ejecutadas_Area_Foscal_Select">
                            <option value="">Seleccione un Área</option>
                        </select>

                        <div>
                            <div id="Indicador_Acciones_Ejecutadas_Area_Foscal"></div>
                        </div>
                    </div>

                    <div id="Area_Anual" class="card-body mt-3 tab-pane fade">
                        <select class="selectpicker" id="Indicador_Acciones_Ejecutadas_Area_Foscal_Anual_Select">
                            <option value="">Seleccione un Área</option>
                        </select>

                        <div>
                            <div id="Indicador_Acciones_Ejecutadas_Area_Foscal_Anual"></div>
                        </div>
                    </div>

                    <div id="Areas_Trimestre" class="card-body mt-3 tab-pane fade">
                        <div>
                            <div id="Indicador_Acciones_Ejecutadas_Areas_Foscal"></div>
                        </div>
                    </div>

                    <div id="Areas_Anual" class="card-body mt-3 tab-pane fade">
                        <div>
                            <div id="Indicador_Acciones_Ejecutadas_Areas_Foscal_Anual"></div>
                        </div>
                    </div>

                </div>
            </div>
        </div>
    </div>

    <div class="row mt-3">
        <div class="col-lg-12 col-12">
            <div class="card shadow mb-4">
                <div class="card-header py-3">
                    <h6 class="m-0 font-weight-bold text-primary">
                        PROPORCIÓN ACCIONES DE FORMACIÓN EJECUTADAS DEL PROGRAMA DE
                        EDUCACIÓN CONTINUA POR AREA
                    </h6>
                    <ul class="nav nav-underline">
                        <li class="nav-item px-2">
                            <a class="nav-link active" data-bs-toggle="tab" href="#Area_Fosunab_Trimestre">Trimestre</a>
                        </li>
                        <li class="nav-item px-2">
                            <a class="nav-link" data-bs-toggle="tab" href="#Area_Fosunab_Anual">Anual</a>
                        </li>
                        <li class="nav-item px-2">
                            <a class="nav-link" data-bs-toggle="tab" href="#Areas_Fosunab_Trimestre">General - Trimestre</a>
                        </li>
                        <li class="nav-item px-2">
                            <a class="nav-link" data-bs-toggle="tab" href="#Areas_Fosunab_Anual">General - Anual</a>
                        </li>
                    </ul>
                </div>
                <div class="fondoDegradado text-center text-white tablaheader">FOSUNAB</div>

                <div class="tab-content">

                    <div id="Area_Fosunab_Trimestre" class="card-body mt-3 tab-pane fade show active">
                        <select class="selectpicker" id="Indicador_Acciones_Ejecutadas_Area_Fosunab_Select">
                            <option value="">Seleccione un Área</option>
                        </select>
                        <div id="Indicador_Acciones_Ejecutadas_Area_Fosunab"></div>
                    </div>

                    <div id="Area_Fosunab_Anual" class="card-body mt-3 tab-pane fade">
                        <select class="selectpicker" id="Indicador_Acciones_Ejecutadas_Area_Fosunab_Anual_Select">
                            <option value="">Seleccione un Área</option>
                        </select>
                        <div id="Indicador_Acciones_Ejecutadas_Area_Fosunab_Anual"></div>
                    </div>

                    <div id="Areas_Fosunab_Trimestre" class="card-body mt-3 tab-pane fade">
                        <div id="Indicador_Acciones_Ejecutadas_Areas_Fosunab"></div>
                    </div>

                    <div id="Areas_Fosunab_Anual" class="card-body mt-3 tab-pane fade">
                        <div id="Indicador_Acciones_Ejecutadas_Areas_Fosunab_Anual"></div>
                    </div>

                </div>
            </div>
        </div>
    </div>

    <div class="row mt-3">
        <div class="col-lg-12 col-12">
            <div class="card shadow mb-4">
                <div class="card-header py-3">
                    <h6 class="m-0 font-weight-bold text-primary">
                        PROPORCIÓN ACCIONES DE FORMACIÓN EJECUTADAS DEL PROGRAMA DE
                        EDUCACIÓN CONTINUA POR CULTURA ORGANIZACIONAL
                    </h6>
                    <ul class="nav nav-underline">
                        <li class="nav-item px-2">
                            <a class="nav-link active" data-bs-toggle="tab" href="#PICO_Trimestre">Trimestre</a>
                        </li>
                        <li class="nav-item px-2">
                            <a class="nav-link" data-bs-toggle="tab" href="#PICO_Anual">Anual</a>
                        </li>
                        <li class="nav-item px-2">
                            <a class="nav-link" data-bs-toggle="tab" href="#PICOs_Trimestre">General - Trimestre</a>
                        </li>
                        <li class="nav-item px-2">
                            <a class="nav-link" data-bs-toggle="tab" href="#PICOs_Anual">General - Anual</a>
                        </li>
                    </ul>
                </div>

                <div class="tab-content">

                    <div id="PICO_Trimestre" class="card-body mt-3 tab-pane fade show active">
                        <select class="selectpicker col-3" id="Indicador_Acciones_Ejecutadas_PICO_Select">
                            <option value="">Seleccione la cultura organizacional</option>
                        </select>
                        <div id="Indicador_Acciones_Ejecutadas_PICO"></div>
                    </div>

                    <div id="PICO_Anual" class="card-body mt-3 tab-pane fade">
                        <select class="selectpicker col-3" id="Indicador_Acciones_Ejecutadas_PICO_Anual_Select">
                            <option value="">Seleccione la cultura organizacional</option>
                        </select>
                        <div id="Indicador_Acciones_Ejecutadas_PICO_Anual"></div>
                    </div>

                    <div id="PICOs_Trimestre" class="card-body mt-3 tab-pane fade">
                        <div id="Indicador_Acciones_Ejecutadas_PICOs"></div>
                    </div>

                    <div id="PICOs_Anual" class="card-body mt-3 tab-pane fade">
                        <div id="Indicador_Acciones_Ejecutadas_PICOs_Anual"></div>
                    </div>

                </div>

            </div>
        </div>
    </div>

    <div class="row mt-3">
        <div class="col-lg-12 col-12">
            <div class="card shadow mb-4">
                <div class="card-header py-3">
                    <h6 class="m-0 font-weight-bold text-primary">
                        PROPORCIÓN ACCIONES DE FORMACIÓN EJECUTADAS DEL PROGRAMA DE
                        EDUCACIÓN CONTINUA POR DESARROLLO INTEGRAL
                    </h6>
                    <ul class="nav nav-underline">
                        <li class="nav-item px-2">
                            <a class="nav-link active" data-bs-toggle="tab" href="#DI_Trimestre">Trimestre</a>
                        </li>
                        <li class="nav-item px-2">
                            <a class="nav-link" data-bs-toggle="tab" href="#DI_Anual">Anual</a>
                        </li>
                    </ul>
                </div>

                <div class="tab-content">
                    <div id="DI_Trimestre" class="card-body mt-3 tab-pane fade show active">
                        <div id="Indicador_Acciones_Ejecutadas_DI"></div>
                    </div>
                    <div id="DI_Anual" class="card-body mt-3 tab-pane fade">
                        <div id="Indicador_Acciones_Ejecutadas_DI_Anual"></div>
                    </div>
                </div>

            </div>
        </div>
    </div>

    <div class="row mt-3">
        <div class="col-lg-12 col-12">
            <div class="card shadow mb-4">
                <div class="card-header py-3">
                    <h6 class="m-0 font-weight-bold text-primary">
                        PROPORCIÓN ACCIONES DE FORMACIÓN EJECUTADAS DEL PROGRAMA DE
                        EDUCACIÓN CONTINUA DE SISTEMA ÚNICO HABILITACIÓN
                    </h6>
                    <ul class="nav nav-underline">
                        <li class="nav-item px-2">
                            <a class="nav-link active" data-bs-toggle="tab" href="#SUH_Trimestre">Trimestre</a>
                        </li>
                        <li class="nav-item px-2">
                            <a class="nav-link" data-bs-toggle="tab" href="#SUH_Anual">Anual</a>
                        </li>
                    </ul>
                </div>

                <div class="tab-content">
                    <div id="SUH_Trimestre" class="card-body mt-3 tab-pane fade show active">
                        <div id="Indicador_Acciones_Ejecutadas_SUH"></div>
                    </div>
                    <div id="SUH_Anual" class="card-body mt-3 tab-pane fade">
                        <div id="Indicador_Acciones_Ejecutadas_SUH_Anual"></div>
                    </div>
                </div>

            </div>
        </div>
    </div>

    <h2 class="colorTitulo text-center">Reportes Asistencia</h2>

    <div class="row mt-3">
        <div class="col-lg-12 col-12">
            <div class="card shadow mb-4">
                <div class="card-header py-3">
                    <h6 class="m-0 font-weight-bold text-primary">
                        PROPORCIÓN ASISTENTES A LAS ACCIONES DE FORMACIÓN DEL PROGRAMA DE EDUCACIÓN CONTINUA
                    </h6>
                    <ul class="nav nav-underline">
                        <li class="nav-item px-2">
                            <a class="nav-link active" data-bs-toggle="tab" href="#A_General_Trimestre">Trimestre</a>
                        </li>
                        <li class="nav-item px-2">
                            <a class="nav-link" data-bs-toggle="tab" href="#A_General_Anual">Anual</a>
                        </li>
                    </ul>
                </div>
                <div class="tab-content">
                    <div id="A_General_Trimestre" class="card-body tab-pane fade show active">
                        <div class="mb-2" id="Indicador_Asistentes_General"></div>
                    </div>
                    <div id="A_General_Anual" class="card-body tab-pane fade">
                        <div class="mb-2" id="Indicador_Asistentes_General_Anual"></div>
                    </div>
                </div>

            </div>
        </div>
    </div>

    <div class="row mt-3">
        <div class="col-lg-12 col-12">
            <div class="card shadow mb-4">
                <div class="card-header py-3">
                    <h6 class="m-0 font-weight-bold text-primary">
                        PROPORCIÓN ASISTENTES A LAS ACCIONES DE FORMACIÓN DEL PROGRAMA DE EDUCACIÓN CONTINUA POR AREA
                    </h6>
                    <ul class="nav nav-underline">
                        <li class="nav-item px-2">
                            <a class="nav-link active" data-bs-toggle="tab" href="#A_Area_Trimestre">Trimestre</a>
                        </li>
                        <li class="nav-item px-2">
                            <a class="nav-link" data-bs-toggle="tab" href="#A_Area_Anual">Anual</a>
                        </li>
                        <li class="nav-item px-2">
                            <a class="nav-link" data-bs-toggle="tab" href="#A_Areas_Trimestre">General - Trimestre</a>
                        </li>
                        <li class="nav-item px-2">
                            <a class="nav-link" data-bs-toggle="tab" href="#A_Areas_Anual">General - Anual</a>
                        </li>
                    </ul>
                </div>
                <div class="fondoDegradado text-center text-white tablaheader">FOSCAL</div>

                <div class="tab-content">

                    <div id="A_Area_Trimestre" class="card-body mt-3 tab-pane fade show active">
                        <select class="selectpicker" id="Indicador_Asistentes_Area_Foscal_Select">
                            <option value="">Seleccione un Área</option>
                        </select>

                        <div>
                            <div id="Indicador_Asistentes_Area_Foscal"></div>
                        </div>
                    </div>

                    <div id="A_Area_Anual" class="card-body mt-3 tab-pane fade">
                        <select class="selectpicker" id="Indicador_Asistentes_Area_Foscal_Anual_Select">
                            <option value="">Seleccione un Área</option>
                        </select>

                        <div>
                            <div id="Indicador_Asistentes_Area_Foscal_Anual"></div>
                        </div>
                    </div>

                    <div id="A_Areas_Trimestre" class="card-body mt-3 tab-pane fade">
                        <div>
                            <div id="Indicador_Asistentes_Areas_Foscal"></div>
                        </div>
                    </div>

                    <div id="A_Areas_Anual" class="card-body mt-3 tab-pane fade">
                        <div>
                            <div id="Indicador_Asistentes_Areas_Foscal_Anual"></div>
                        </div>
                    </div>

                </div>
            </div>
        </div>
    </div>

    <div class="row mt-3">
        <div class="col-lg-12 col-12">
            <div class="card shadow mb-4">
                <div class="card-header py-3">
                    <h6 class="m-0 font-weight-bold text-primary">
                        PROPORCIÓN ASISTENTES A LAS ACCIONES DE FORMACIÓN DEL PROGRAMA DE EDUCACIÓN CONTINUA POR AREA
                    </h6>
                    <ul class="nav nav-underline">
                        <li class="nav-item px-2">
                            <a class="nav-link active" data-bs-toggle="tab" href="#A_Area_Fosunab_Trimestre">Trimestre</a>
                        </li>
                        <li class="nav-item px-2">
                            <a class="nav-link" data-bs-toggle="tab" href="#A_Area_Fosunab_Anual">Anual</a>
                        </li>
                        <li class="nav-item px-2">
                            <a class="nav-link" data-bs-toggle="tab" href="#A_Areas_Fosunab_Trimestre">General - Trimestre</a>
                        </li>
                        <li class="nav-item px-2">
                            <a class="nav-link" data-bs-toggle="tab" href="#A_Areas_Fosunab_Anual">General - Anual</a>
                        </li>
                    </ul>
                </div>
                <div class="fondoDegradado text-center text-white tablaheader">FOSUNAB</div>

                <div class="tab-content">

                    <div id="A_Area_Fosunab_Trimestre" class="card-body mt-3 tab-pane fade show active">
                        <select class="selectpicker" id="Indicador_Asistentes_Area_Fosunab_Select">
                            <option value="">Seleccione un Área</option>
                        </select>
                        <div id="Indicador_Asistentes_Area_Fosunab"></div>
                    </div>

                    <div id="A_Area_Fosunab_Anual" class="card-body mt-3 tab-pane fade">
                        <select class="selectpicker" id="Indicador_Asistentes_Area_Fosunab_Anual_Select">
                            <option value="">Seleccione un Área</option>
                        </select>
                        <div id="Indicador_Asistentes_Area_Fosunab_Anual"></div>
                    </div>

                    <div id="A_Areas_Fosunab_Trimestre" class="card-body mt-3 tab-pane fade">
                        <div id="Indicador_Asistentes_Areas_Fosunab"></div>
                    </div>

                    <div id="A_Areas_Fosunab_Anual" class="card-body mt-3 tab-pane fade">
                        <div id="Indicador_Asistentes_Areas_Fosunab_Anual"></div>
                    </div>

                </div>
            </div>
        </div>
    </div>

    <div class="row mt-3">
        <div class="col-lg-12 col-12">
            <div class="card shadow mb-4">
                <div class="card-header py-3">
                    <h6 class="m-0 font-weight-bold text-primary">
                        PROPORCIÓN ASISTENTES A LAS ACCIONES DE FORMACIÓN DEL PROGRAMA DE EDUCACIÓN CONTINUA POR CULTURA ORGANIZACIONAL
                    </h6>
                    <ul class="nav nav-underline">
                        <li class="nav-item px-2">
                            <a class="nav-link active" data-bs-toggle="tab" href="#A_PICO_Trimestre">Trimestre</a>
                        </li>
                        <li class="nav-item px-2">
                            <a class="nav-link" data-bs-toggle="tab" href="#A_PICO_Anual">Anual</a>
                        </li>
                        <li class="nav-item px-2">
                            <a class="nav-link" data-bs-toggle="tab" href="#A_PICOs_Trimestre">General - Trimestre</a>
                        </li>
                        <li class="nav-item px-2">
                            <a class="nav-link" data-bs-toggle="tab" href="#A_PICOs_Anual">General - Anual</a>
                        </li>
                    </ul>
                </div>

                <div class="tab-content">

                    <div id="A_PICO_Trimestre" class="card-body mt-3 tab-pane fade show active">
                        <select class="selectpicker col-3" id="Indicador_Asistentes_PICO_Select">
                            <option value="">Seleccione la cultura organizacional</option>
                        </select>
                        <div id="Indicador_Asistentes_PICO"></div>
                    </div>

                    <div id="A_PICO_Anual" class="card-body mt-3 tab-pane fade">
                        <select class="selectpicker col-3" id="Indicador_Asistentes_PICO_Anual_Select">
                            <option value="">Seleccione la cultura organizacional</option>
                        </select>
                        <div id="Indicador_Asistentes_PICO_Anual"></div>
                    </div>

                    <div id="A_PICOs_Trimestre" class="card-body mt-3 tab-pane fade">
                        <div id="Indicador_Asistentes_PICOs"></div>
                    </div>

                    <div id="A_PICOs_Anual" class="card-body mt-3 tab-pane fade">
                        <div id="Indicador_Asistentes_PICOs_Anual"></div>
                    </div>

                </div>

            </div>
        </div>
    </div>

    <div class="row mt-3">
        <div class="col-lg-12 col-12">
            <div class="card shadow mb-4">
                <div class="card-header py-3">
                    <h6 class="m-0 font-weight-bold text-primary">
                        PROPORCIÓN ASISTENTES A LAS ACCIONES DE FORMACIÓN DEL PROGRAMA DE EDUCACIÓN CONTINUA POR DESARROLLO INTEGRAL
                    </h6>
                    <ul class="nav nav-underline">
                        <li class="nav-item px-2">
                            <a class="nav-link active" data-bs-toggle="tab" href="#A_DI_Trimestre">Trimestre</a>
                        </li>
                        <li class="nav-item px-2">
                            <a class="nav-link" data-bs-toggle="tab" href="#A_DI_Anual">Anual</a>
                        </li>
                    </ul>
                </div>

                <div class="tab-content">
                    <div id="A_DI_Trimestre" class="card-body mt-3 tab-pane fade show active">
                        <div id="Indicador_Asistentes_DI"></div>
                    </div>
                    <div id="A_DI_Anual" class="card-body mt-3 tab-pane fade">
                        <div id="Indicador_Asistentes_DI_Anual"></div>
                    </div>
                </div>

            </div>
        </div>
    </div>

    <div class="row mt-3">
        <div class="col-lg-12 col-12">
            <div class="card shadow mb-4">
                <div class="card-header py-3">
                    <h6 class="m-0 font-weight-bold text-primary">
                        PROPORCIÓN ASISTENTES A LAS ACCIONES DE FORMACIÓN DEL PROGRAMA DE EDUCACIÓN CONTINUA DE SISTEMA ÚNICO HABILITACIÓN
                    </h6>
                    <ul class="nav nav-underline">
                        <li class="nav-item px-2">
                            <a class="nav-link active" data-bs-toggle="tab" href="#A_SUH_Trimestre">Trimestre</a>
                        </li>
                        <li class="nav-item px-2">
                            <a class="nav-link" data-bs-toggle="tab" href="#A_SUH_Anual">Anual</a>
                        </li>
                    </ul>
                </div>

                <div class="tab-content">
                    <div id="A_SUH_Trimestre" class="card-body mt-3 tab-pane fade show active">
                        <div id="Indicador_Asistentes_SUH"></div>
                    </div>
                    <div id="A_SUH_Anual" class="card-body mt-3 tab-pane fade">
                        <div id="Indicador_Asistentes_SUH_Anual"></div>
                    </div>
                </div>

            </div>
        </div>
    </div>

    <h2 class="colorTitulo text-center">Reportes de EduFoscal</h2>

    <div class="row mt-3 pe-5 ps-5">
        <button class="bi bi-graph-up btn btn-outline-primary mb-2" onclick="javascript: actualizarDatosEduFoscal();"> ACTUALIZAR DATOS EDUFOSCAL</button>
    </div>


    <div class="row mt-3">
        <div class="col-lg-12 col-12">
            <div class="card shadow mb-4">
                <div class="card-header py-3">
                    <h6 class="m-0 font-weight-bold text-primary">
                        PROPORCIÓN ACCIONES DE FORMACIÓN EJECUTADAS DEL PROGRAMA DE
                        EDUCACIÓN CONTINUA
                    </h6>
                    <ul class="nav nav-underline">
                        <li class="nav-item px-2">
                            <a class="nav-link active" data-bs-toggle="tab" href="#E_General_Trimestre">Trimestre</a>
                        </li>
                        <li class="nav-item px-2">
                            <a class="nav-link" data-bs-toggle="tab" href="#E_General_Anual">Anual</a>
                        </li>
                    </ul>
                </div>
                <div class="tab-content">
                    <div id="E_General_Trimestre" class="card-body tab-pane fade show active">
                        <div class="mb-2" id="Indicador_Acciones_Ejecutadas_General_EduFoscal"></div>
                    </div>
                    <div id="E_General_Anual" class="card-body tab-pane fade">
                        <div class="mb-2" id="Indicador_Acciones_Ejecutadas_General_EduFoscal_Anual"></div>
                    </div>
                </div>

            </div>
        </div>
    </div>

    <div class="row mt-3">
        <div class="col-lg-12 col-12">
            <div class="card shadow mb-4">
                <div class="card-header py-3">
                    <h6 class="m-0 font-weight-bold text-primary">
                        PROPORCIÓN ASISTENTES A LAS ACCIONES DE FORMACIÓN DEL PROGRAMA DE EDUCACIÓN CONTINUA
                    </h6>
                    <ul class="nav nav-underline">
                        <li class="nav-item px-2">
                            <a class="nav-link active" data-bs-toggle="tab" href="#E_A_General_Trimestre">Trimestre</a>
                        </li>
                        <li class="nav-item px-2">
                            <a class="nav-link" data-bs-toggle="tab" href="#E_A_General_Anual">Anual</a>
                        </li>
                    </ul>
                </div>
                <div class="tab-content">
                    <div id="E_A_General_Trimestre" class="card-body tab-pane fade show active">
                        <div class="mb-2" id="Indicador_Asistentes_General_EduFoscal"></div>
                    </div>
                    <div id="E_A_General_Anual" class="card-body tab-pane fade">
                        <div class="mb-2" id="Indicador_Asistentes_General_EduFoscal_Anual"></div>
                    </div>
                </div>

            </div>
        </div>
    </div>

</div>

<script>
    function actualizarDatosEduFoscal() {
        try {
            $.ajax({
                url: "ActualizarDatosCursosEduFoscal",
                type: "post",
                success: function (result) {
                    alertify.alert(result, function () { location.reload(); });
                }, function(xhr, status, error) { alert(error); },
            });
        } catch (e) { alert(e); }
    }
</script>

<script>
    $(document).ready(function () {
        google.charts.load('current', { 'packages': ['corechart', 'controls'] });
        google.charts.load('current', { packages: ['table'] });
        google.load('visualization', '1', { packages: ['controls'] });
        google.charts.load("current", { packages: ['bar'] });
        google.charts.setOnLoadCallback(ObtenerDatosGraficas);
        //google.charts.setOnLoadCallback(function () {
        //    var Results = {
        //        IndicadorAccionesEjecutadas: [
        //            { Trimestre: "Primer Trimestre", Indicador: 0.75, F: 0.1 },
        //            { Trimestre: "Segundo Trimestre", Indicador: 0.60, F: 0.1 },
        //            { Trimestre: "Tercer Trimestre", Indicador: 0.55, F: 0.1 },
        //            { Trimestre: "Cuarto Trimestre", Indicador: 0.70, F: 0.1 }
        //        ]
        //    };
        //    GraficarIndicadorAccionesEjecutadasArea(Results);
        //});
    });

    var data_IndicadorAccionesEjecutadasAreaFoscal;
    var data_IndicadorAccionesEjecutadasAreaFosunab;
    var data_IndicadorAccionesEjecutadasAreasFoscal;
    var data_IndicadorAccionesEjecutadasAreasFosunab;
    var data_IndicadorAccionesEjecutadasPICO;
    var data_IndicadorAccionesEjecutadasPICOs;

    var data_IndicadorAccionesEjecutadasAreaFoscalAnual;
    var data_IndicadorAccionesEjecutadasAreaFosunabAnual;
    var data_IndicadorAccionesEjecutadasAreasFoscalAnual;
    var data_IndicadorAccionesEjecutadasAreasFosunabAnual;
    var data_IndicadorAccionesEjecutadasPICOAnual;
    var data_IndicadorAccionesEjecutadasPICOsAnual;
    ///////////////////////////////////////////////////////////////
    var data_IndicadorAsistentesAreaFoscal;
    var data_IndicadorAsistentesAreaFosunab;
    var data_IndicadorAsistentesAreasFoscal;
    var data_IndicadorAsistentesAreasFosunab;
    var data_IndicadorAsistentesPICO;
    var data_IndicadorAsistentesPICOs;

    var data_IndicadorAsistentesAreaFoscalAnual;
    var data_IndicadorAsistentesAreaFosunabAnual;
    var data_IndicadorAsistentesAreasFoscalAnual;
    var data_IndicadorAsistentesAreasFosunabAnual;
    var data_IndicadorAsistentesPICOAnual;
    var data_IndicadorAsistentesPICOsAnual;


    function ObtenerDatosGraficas() {
        try {
            $.ajax({
                url: "DatosGraficas",
                type: "post",
                success: function (Results) {
                    if (Results != undefined || Results != null) {
                        ///////////////////////////INDICADORES TRIMESTRE//////////////////////////////////
                        renderChartIndicadorAccionesEjecutadasGeneral(Results.IndicadorAccionesEjecutadasGeneral);
                        //
                        data_IndicadorAccionesEjecutadasAreaFoscal = Results.IndicadorAccionesEjecutadasAreaFoscal;
                        renderSelectIndicadorAccionesEjecutadasAreaFoscal();
                        renderChartIndicadorAccionesEjecutadasAreaFoscal(data_IndicadorAccionesEjecutadasAreaFoscal);
                        //
                        data_IndicadorAccionesEjecutadasAreasFoscal = Results.IndicadorAccionesEjecutadasAreasFoscal;
                        renderChartIndicadorAccionesEjecutadasAreasFoscal(data_IndicadorAccionesEjecutadasAreasFoscal);
                        //
                        data_IndicadorAccionesEjecutadasAreaFosunab = Results.IndicadorAccionesEjecutadasAreaFosunab;
                        renderSelectIndicadorAccionesEjecutadasAreaFosunab();
                        renderChartIndicadorAccionesEjecutadasAreaFosunab(data_IndicadorAccionesEjecutadasAreaFosunab);
                        //
                        data_IndicadorAccionesEjecutadasAreasFosunab = Results.IndicadorAccionesEjecutadasAreasFosunab;
                        renderChartIndicadorAccionesEjecutadasAreasFosunab(data_IndicadorAccionesEjecutadasAreasFosunab);
                        //
                        data_IndicadorAccionesEjecutadasPICO = Results.IndicadorAccionesEjecutadasPICO;
                        renderSelectIndicadorAccionesEjecutadasPICO();
                        renderChartIndicadorAccionesEjecutadasPICO(data_IndicadorAccionesEjecutadasPICO);
                        //
                        data_IndicadorAccionesEjecutadasPICOs = Results.IndicadorAccionesEjecutadasPICOs;
                        renderChartIndicadorAccionesEjecutadasPICOs(data_IndicadorAccionesEjecutadasPICOs);
                        //
                        renderChartIndicadorAccionesEjecutadasDI(Results.IndicadorAccionesEjecutadasDI);
                        //
                        renderChartIndicadorAccionesEjecutadasSUH(Results.IndicadorAccionesEjecutadasSUH);
                        ///////////////////////////INDICADORES ANUALES/////////////////////////
                        renderChartIndicadorAccionesEjecutadasGeneralAnual(Results);
                        //
                        data_IndicadorAccionesEjecutadasAreaFoscalAnual = Results.IndicadorAccionesEjecutadasAreaFoscalAnual;
                        renderSelectIndicadorAccionesEjecutadasAreaFoscalAnual();
                        renderChartIndicadorAccionesEjecutadasAreaFoscalAnual(data_IndicadorAccionesEjecutadasAreaFoscalAnual);
                        //
                        data_IndicadorAccionesEjecutadasAreasFoscalAnual = Results.IndicadorAccionesEjecutadasAreasFoscalAnual;
                        renderChartIndicadorAccionesEjecutadasAreasFoscalAnual(data_IndicadorAccionesEjecutadasAreasFoscalAnual);
                        //
                        data_IndicadorAccionesEjecutadasAreaFosunabAnual = Results.IndicadorAccionesEjecutadasAreaFosunabAnual;
                        renderSelectIndicadorAccionesEjecutadasAreaFosunabAnual();
                        renderChartIndicadorAccionesEjecutadasAreaFosunabAnual(data_IndicadorAccionesEjecutadasAreaFosunabAnual);
                        //
                        data_IndicadorAccionesEjecutadasAreasFosunabAnual = Results.IndicadorAccionesEjecutadasAreasFosunabAnual;
                        renderChartIndicadorAccionesEjecutadasAreasFosunabAnual(data_IndicadorAccionesEjecutadasAreasFosunabAnual);
                        //
                        data_IndicadorAccionesEjecutadasPICOAnual = Results.IndicadorAccionesEjecutadasPICOAnual;
                        renderSelectIndicadorAccionesEjecutadasPICOAnual();
                        renderChartIndicadorAccionesEjecutadasPICOAnual(data_IndicadorAccionesEjecutadasPICOAnual);
                        //
                        data_IndicadorAccionesEjecutadasPICOsAnual = Results.IndicadorAccionesEjecutadasPICOsAnual;
                        renderChartIndicadorAccionesEjecutadasPICOsAnual(data_IndicadorAccionesEjecutadasPICOsAnual);
                        //
                        renderChartIndicadorAccionesEjecutadasDIAnual(Results.IndicadorAccionesEjecutadasDIAnual);
                        //
                        renderChartIndicadorAccionesEjecutadasSUHAnual(Results.IndicadorAccionesEjecutadasSUHAnual);
                        //////////////////////////////////ASISTENTES TRIMESTRE///////////////////////////////////////////
                        renderChartIndicadorAsistentesGeneral(Results.IndicadorAsistentesGeneral);
                        //
                        data_IndicadorAsistentesAreaFoscal = Results.IndicadorAsistentesAreaFoscal;
                        renderSelectIndicadorAsistentesAreaFoscal();
                        renderChartIndicadorAsistentesAreaFoscal(data_IndicadorAsistentesAreaFoscal);
                        //
                        data_IndicadorAsistentesAreasFoscal = Results.IndicadorAsistentesAreasFoscal;
                        renderChartIndicadorAsistentesAreasFoscal(data_IndicadorAsistentesAreasFoscal);
                        //
                        data_IndicadorAsistentesAreaFosunab = Results.IndicadorAsistentesAreaFosunab;
                        renderSelectIndicadorAsistentesAreaFosunab();
                        renderChartIndicadorAsistentesAreaFosunab(data_IndicadorAsistentesAreaFosunab);
                        //
                        data_IndicadorAsistentesAreasFosunab = Results.IndicadorAsistentesAreasFosunab;
                        renderChartIndicadorAsistentesAreasFosunab(data_IndicadorAsistentesAreasFosunab);
                        //
                        data_IndicadorAsistentesPICO = Results.IndicadorAsistentesPICO;
                        renderSelectIndicadorAsistentesPICO();
                        renderChartIndicadorAsistentesPICO(data_IndicadorAsistentesPICO);
                        //
                        data_IndicadorAsistentesPICOs = Results.IndicadorAsistentesPICOs;
                        renderChartIndicadorAsistentesPICOs(data_IndicadorAsistentesPICOs);
                        //
                        renderChartIndicadorAsistentesDI(Results.IndicadorAsistentesDI);
                        //
                        renderChartIndicadorAsistentesSUH(Results.IndicadorAsistentesSUH);
                        ////////////////ASISTENTES ANUAL////////////////////
                        renderChartIndicadorAsistentesGeneralAnual(Results);
                        //
                        data_IndicadorAsistentesAreaFoscalAnual = Results.IndicadorAsistentesAreaFoscalAnual;
                        renderSelectIndicadorAsistentesAreaFoscalAnual();
                        renderChartIndicadorAsistentesAreaFoscalAnual(data_IndicadorAsistentesAreaFoscalAnual);
                        //
                        data_IndicadorAsistentesAreasFoscalAnual = Results.IndicadorAsistentesAreasFoscalAnual;
                        renderChartIndicadorAsistentesAreasFoscalAnual(data_IndicadorAsistentesAreasFoscalAnual);
                        //
                        data_IndicadorAsistentesAreaFosunabAnual = Results.IndicadorAsistentesAreaFosunabAnual;
                        renderSelectIndicadorAsistentesAreaFosunabAnual();
                        renderChartIndicadorAsistentesAreaFosunabAnual(data_IndicadorAsistentesAreaFosunabAnual);
                        //
                        data_IndicadorAsistentesAreasFosunabAnual = Results.IndicadorAsistentesAreasFosunabAnual;
                        renderChartIndicadorAsistentesAreasFosunabAnual(data_IndicadorAsistentesAreasFosunabAnual);
                        //
                        data_IndicadorAsistentesPICOAnual = Results.IndicadorAsistentesPICOAnual;
                        renderSelectIndicadorAsistentesPICOAnual();
                        renderChartIndicadorAsistentesPICOAnual(data_IndicadorAsistentesPICOAnual);
                        //
                        data_IndicadorAsistentesPICOsAnual = Results.IndicadorAsistentesPICOsAnual;
                        renderChartIndicadorAsistentesPICOsAnual(data_IndicadorAsistentesPICOsAnual);
                        //
                        renderChartIndicadorAsistentesDIAnual(Results.IndicadorAsistentesDIAnual);
                        //
                        renderChartIndicadorAsistentesSUHAnual(Results.IndicadorAsistentesSUHAnual);


                        /////////////////////INDICADORES EDUFOSCAL//////////////////////////////
                        renderChartIndicadorAccionesEjecutadasGeneralEduFoscal(Results.IndicadorAccionesEjecutadasGeneralEduFoscal);

                        renderChartIndicadorAccionesEjecutadasGeneralEduFoscalAnual(Results);

                        renderChartIndicadorAsistentesGeneralEduFoscal(Results.IndicadorAsistentesGeneralEduFoscal);

                        renderChartIndicadorAsistentesGeneralEduFoscalAnual(Results);
                    }
                },
                error: function (xhr, status, error) { alert(error); },
            });
        }
        catch (e) { alert(e); }
    }
    ///////////////////INDICADORES TRIMESTRAL/////////////////
    function renderChartIndicadorAccionesEjecutadasGeneral(filteredData) {
        var dataTable = new google.visualization.DataTable();
        dataTable.addColumn('string', 'Trimestre');
        dataTable.addColumn('number', 'FOSCAL');
        dataTable.addColumn('number', 'FOSUNAB');

        var trimestres = ['Primer Trimestre', 'Segundo Trimestre', 'Tercer Trimestre', 'Cuarto Trimestre'];

        trimestres.forEach(trimestreName => {
            var dataForTrimestre = filteredData.filter(item => item.Trimestre === trimestreName);

            var totalFoscal = dataForTrimestre.reduce((sum, item) => sum + item.Foscal, 0);
            var totalFosunab = dataForTrimestre.reduce((sum, item) => sum + item.Fosunab, 0);
            dataTable.addRow([trimestreName, totalFoscal, totalFosunab]);
        });

        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#3366CC', '#F49004'],
            chartArea: { width: '70%' }
        };
        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Acciones_Ejecutadas_General'));
        chart.draw(dataTable, options);
    }
    //
    function renderSelectIndicadorAccionesEjecutadasAreaFoscal() {
        var areas = [...new Set(data_IndicadorAccionesEjecutadasAreaFoscal.map(item => item.Area))];
        var select = document.getElementById('Indicador_Acciones_Ejecutadas_Area_Foscal_Select');
        select.innerHTML = '<option value="">Seleccione un Área</option>';
        areas.forEach(area => {
            var option = document.createElement('option');
            option.value = area;
            option.textContent = area;
            select.appendChild(option);
        });
        $('.selectpicker').selectpicker("refresh");
        select.addEventListener('change', function () {
            var selectedArea = this.value;
            if (selectedArea) {
                renderChartIndicadorAccionesEjecutadasAreaFoscal(data_IndicadorAccionesEjecutadasAreaFoscal.filter(item => item.Area === selectedArea));
            }
        });
    }
    
    function renderChartIndicadorAccionesEjecutadasAreaFoscal(filteredData) {
        var dataTable = new google.visualization.DataTable();
        dataTable.addColumn('string', 'Trimestre');
        dataTable.addColumn('number', 'FOSCAL');

        var trimestres = ['Primer Trimestre', 'Segundo Trimestre', 'Tercer Trimestre', 'Cuarto Trimestre'];
        trimestres.forEach(trimestreName => {
            var indicador = filteredData.find(item => item.Trimestre === trimestreName)?.Indicador || 0;
            dataTable.addRow([trimestreName, indicador]);
        });

        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#3366CC', '#F49004'],
            chartArea: { width: '70%' }
        };

        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Acciones_Ejecutadas_Area_Foscal'));
        chart.draw(dataTable, options);
    }
    //
    function renderChartIndicadorAccionesEjecutadasAreasFoscal(filteredData) {
        var dataTable = new google.visualization.DataTable();
        dataTable.addColumn('string', 'Trimestre');
        dataTable.addColumn('number', 'FOSCAL');

        var trimestres = ['Primer Trimestre', 'Segundo Trimestre', 'Tercer Trimestre', 'Cuarto Trimestre'];
        trimestres.forEach(trimestreName => {
            var indicador = filteredData.find(item => item.Trimestre === trimestreName)?.Indicador || 0;
            dataTable.addRow([trimestreName, indicador]);
        });

        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#3366CC'],
            chartArea: { width: '70%' }
        };

        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Acciones_Ejecutadas_Areas_Foscal'));
        chart.draw(dataTable, options);
    }
    //
    function renderSelectIndicadorAccionesEjecutadasAreaFosunab() {
        var areas = [...new Set(data_IndicadorAccionesEjecutadasAreaFosunab.map(item => item.Area))];
        var select = document.getElementById('Indicador_Acciones_Ejecutadas_Area_Fosunab_Select');
        select.innerHTML = '<option value="">Seleccione un Área</option>';
        areas.forEach(area => {
            var option = document.createElement('option');
            option.value = area;
            option.textContent = area;
            select.appendChild(option);
        });
        $('.selectpicker').selectpicker("refresh");
        select.addEventListener('change', function () {
            var selectedArea = this.value;
            if (selectedArea) {
                renderChartIndicadorAccionesEjecutadasAreaFosunab(data_IndicadorAccionesEjecutadasAreaFosunab.filter(item => item.Area === selectedArea));
            }
        });
    }

    function renderChartIndicadorAccionesEjecutadasAreaFosunab(filteredData) {
        var dataTable = new google.visualization.DataTable();
        dataTable.addColumn('string', 'Trimestre');
        dataTable.addColumn('number', 'FOSUNAB');

        var trimestres = ['Primer Trimestre', 'Segundo Trimestre', 'Tercer Trimestre', 'Cuarto Trimestre'];
        trimestres.forEach(trimestreName => {
            var indicador = filteredData.find(item => item.Trimestre === trimestreName)?.Indicador || 0;
            dataTable.addRow([trimestreName, indicador]);
        });

        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#F49004'],
            chartArea: { width: '70%' }
        };

        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Acciones_Ejecutadas_Area_Fosunab'));
        chart.draw(dataTable, options);
    }
    //
    function renderChartIndicadorAccionesEjecutadasAreasFosunab(filteredData) {
        var dataTable = new google.visualization.DataTable();
        dataTable.addColumn('string', 'Trimestre');
        dataTable.addColumn('number', 'FOSUNAB');

        var trimestres = ['Primer Trimestre', 'Segundo Trimestre', 'Tercer Trimestre', 'Cuarto Trimestre'];
        trimestres.forEach(trimestreName => {
            var indicador = filteredData.find(item => item.Trimestre === trimestreName)?.Indicador || 0;
            dataTable.addRow([trimestreName, indicador]);
        });

        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#F49004'],
            chartArea: { width: '70%' }
        };

        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Acciones_Ejecutadas_Areas_Fosunab'));
        chart.draw(dataTable, options);
    }
    //
    function renderSelectIndicadorAccionesEjecutadasPICO() {
        var areas = [...new Set(data_IndicadorAccionesEjecutadasPICO.map(item => item.PICO))];
        var select = document.getElementById('Indicador_Acciones_Ejecutadas_PICO_Select');
        select.innerHTML = '<option value="">Seleccione la cultura organizacional</option>';
        areas.forEach(area => {
            var option = document.createElement('option');
            option.value = area;
            option.textContent = area;
            select.appendChild(option);
        });
        $('.selectpicker').selectpicker("refresh");
        select.addEventListener('change', function () {
            var selectedArea = this.value;
            if (selectedArea) {
                renderChartIndicadorAccionesEjecutadasPICO(data_IndicadorAccionesEjecutadasPICO.filter(item => item.PICO === selectedArea));
            }
        });
    }

    function renderChartIndicadorAccionesEjecutadasPICO(filteredData) {
        var dataTable = new google.visualization.DataTable();
        dataTable.addColumn('string', 'Trimestre');
        dataTable.addColumn('number', 'FOSCAL');
        dataTable.addColumn('number', 'FOSUNAB');

        var trimestres = ['Primer Trimestre', 'Segundo Trimestre', 'Tercer Trimestre', 'Cuarto Trimestre'];

        trimestres.forEach(trimestreName => {
            var dataForTrimestre = filteredData.filter(item => item.Trimestre === trimestreName);

            var totalFoscal = dataForTrimestre.reduce((sum, item) => sum + item.Foscal, 0);
            var totalFosunab = dataForTrimestre.reduce((sum, item) => sum + item.Fosunab, 0);
            dataTable.addRow([trimestreName, totalFoscal, totalFosunab]);
        });

        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#3366CC', '#F49004'],
            chartArea: { width: '70%' }
        };

        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Acciones_Ejecutadas_PICO'));
        chart.draw(dataTable, options);
    }
    //
    function renderChartIndicadorAccionesEjecutadasPICOs(filteredData) {
        var dataTable = new google.visualization.DataTable();
        dataTable.addColumn('string', 'Trimestre');
        dataTable.addColumn('number', 'FOSCAL');
        dataTable.addColumn('number', 'FOSUNAB');

        var trimestres = ['Primer Trimestre', 'Segundo Trimestre', 'Tercer Trimestre', 'Cuarto Trimestre'];

        trimestres.forEach(trimestreName => {
            var dataForTrimestre = filteredData.filter(item => item.Trimestre === trimestreName);

            var totalFoscal = dataForTrimestre.reduce((sum, item) => sum + item.Foscal, 0);
            var totalFosunab = dataForTrimestre.reduce((sum, item) => sum + item.Fosunab, 0);
            dataTable.addRow([trimestreName, totalFoscal, totalFosunab]);
        });

        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#3366CC', '#F49004'],
            chartArea: { width: '70%' }
        };

        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Acciones_Ejecutadas_PICOs'));
        chart.draw(dataTable, options);
    }
    //
    function renderChartIndicadorAccionesEjecutadasDI(filteredData) {
        var dataTable = new google.visualization.DataTable();
        dataTable.addColumn('string', 'Trimestre');
        dataTable.addColumn('number', 'FOSCAL');
        dataTable.addColumn('number', 'FOSUNAB');

        var trimestres = ['Primer Trimestre', 'Segundo Trimestre', 'Tercer Trimestre', 'Cuarto Trimestre'];

        trimestres.forEach(trimestreName => {
            var dataForTrimestre = filteredData.filter(item => item.Trimestre === trimestreName);

            var totalFoscal = dataForTrimestre.reduce((sum, item) => sum + item.Foscal, 0);
            var totalFosunab = dataForTrimestre.reduce((sum, item) => sum + item.Fosunab, 0);
            dataTable.addRow([trimestreName, totalFoscal, totalFosunab]);
        });

        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#3366CC', '#F49004'],
            chartArea: { width: '70%' }
        };

        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Acciones_Ejecutadas_DI'));
        chart.draw(dataTable, options);
    }
    //
    function renderChartIndicadorAccionesEjecutadasSUH(filteredData) {
        var dataTable = new google.visualization.DataTable();
        dataTable.addColumn('string', 'Trimestre');
        dataTable.addColumn('number', 'FOSCAL');
        dataTable.addColumn('number', 'FOSUNAB');

        var trimestres = ['Primer Trimestre', 'Segundo Trimestre', 'Tercer Trimestre', 'Cuarto Trimestre'];

        trimestres.forEach(trimestreName => {
            var dataForTrimestre = filteredData.filter(item => item.Trimestre === trimestreName);

            var totalFoscal = dataForTrimestre.reduce((sum, item) => sum + item.Foscal, 0);
            var totalFosunab = dataForTrimestre.reduce((sum, item) => sum + item.Fosunab, 0);
            dataTable.addRow([trimestreName, totalFoscal, totalFosunab]);
        });

        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#3366CC', '#F49004'],
            chartArea: { width: '70%' }
        };

        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Acciones_Ejecutadas_SUH'));
        chart.draw(dataTable, options);
    }
    ////////////////INDICADORES ANUAL//////////////////////
    function renderChartIndicadorAccionesEjecutadasGeneralAnual(Results) {
        var data2 = [['Año', 'FOSCAL', 'FOSUNAB']];
        for (i = 0; i < Results.IndicadorAccionesEjecutadasGeneralAnual.length; i++) {
            var item = Results.IndicadorAccionesEjecutadasGeneralAnual[i];
            data2.push([item.Year, item.Foscal, item.Fosunab]);
        }
        var datos = new google.visualization.arrayToDataTable(data2);
        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#3366CC', '#F49004'],
            chartArea: { width: '70%' }
        };
        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Acciones_Ejecutadas_General_Anual'));
        chart.draw(datos, options);
    }
    //
    function renderSelectIndicadorAccionesEjecutadasAreaFoscalAnual() {
        var areas = [...new Set(data_IndicadorAccionesEjecutadasAreaFoscalAnual.map(item => item.Area))];
        var select = document.getElementById('Indicador_Acciones_Ejecutadas_Area_Foscal_Anual_Select');
        select.innerHTML = '<option value="">Seleccione un Área</option>';
        areas.forEach(area => {
            var option = document.createElement('option');
            option.value = area;
            option.textContent = area;
            select.appendChild(option);
        });
        $('.selectpicker').selectpicker("refresh");
        select.addEventListener('change', function () {
            var selectedArea = this.value;
            if (selectedArea) {
                renderChartIndicadorAccionesEjecutadasAreaFoscalAnual(data_IndicadorAccionesEjecutadasAreaFoscalAnual.filter(item => item.Area === selectedArea));
            }
        });
    }

    function renderChartIndicadorAccionesEjecutadasAreaFoscalAnual(filteredData) {
        var dataTable = new google.visualization.DataTable();
        dataTable.addColumn('string', 'Año');
        dataTable.addColumn('number', 'FOSCAL');

        if (filteredData.length != 0) {
            var trimestres = [filteredData[0].Year];
            trimestres.forEach(trimestreName => {
                var indicador = filteredData.find(item => item.Year === trimestreName)?.Indicador || 0;
                dataTable.addRow([trimestreName, indicador]);
            });
        }

        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#3366CC', '#F49004'],
            chartArea: { width: '70%' }
        };

        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Acciones_Ejecutadas_Area_Foscal_Anual'));
        chart.draw(dataTable, options);
    }
    //
    function renderChartIndicadorAccionesEjecutadasAreasFoscalAnual(filteredData) {
        var dataTable = new google.visualization.DataTable();
        dataTable.addColumn('string', 'Año');
        dataTable.addColumn('number', 'FOSCAL');

        if (filteredData.length != 0) {
            var trimestres = [filteredData[0].Year];
            trimestres.forEach(trimestreName => {
                var indicador = filteredData.find(item => item.Year === trimestreName)?.Indicador || 0;
                dataTable.addRow([trimestreName, indicador]);
            });
        }

        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#3366CC', '#F49004'],
            chartArea: { width: '70%' }
        };

        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Acciones_Ejecutadas_Areas_Foscal_Anual'));
        chart.draw(dataTable, options);
    }
    //
    function renderSelectIndicadorAccionesEjecutadasAreaFosunabAnual() {
        var areas = [...new Set(data_IndicadorAccionesEjecutadasAreaFosunabAnual.map(item => item.Area))];
        var select = document.getElementById('Indicador_Acciones_Ejecutadas_Area_Fosunab_Anual_Select');
        select.innerHTML = '<option value="">Seleccione un Área</option>';
        areas.forEach(area => {
            var option = document.createElement('option');
            option.value = area;
            option.textContent = area;
            select.appendChild(option);
        });
        $('.selectpicker').selectpicker("refresh");
        select.addEventListener('change', function () {
            var selectedArea = this.value;
            if (selectedArea) {
                renderChartIndicadorAccionesEjecutadasAreaFosunabAnual(data_IndicadorAccionesEjecutadasAreaFosunabAnual.filter(item => item.Area === selectedArea));
            }
        });
    }

    function renderChartIndicadorAccionesEjecutadasAreaFosunabAnual(filteredData) {
        var dataTable = new google.visualization.DataTable();
        dataTable.addColumn('string', 'Año');
        dataTable.addColumn('number', 'FOSUNAB');

        if (filteredData.length != 0) {
            var trimestres = [filteredData[0].Year];
            trimestres.forEach(trimestreName => {
                var indicador = filteredData.find(item => item.Year === trimestreName)?.Indicador || 0;
                dataTable.addRow([trimestreName, indicador]);
            });
        }

        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#F49004'],
            chartArea: { width: '70%' }
        };

        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Acciones_Ejecutadas_Area_Fosunab_Anual'));
        chart.draw(dataTable, options);
    }
    //
    function renderChartIndicadorAccionesEjecutadasAreasFosunabAnual(filteredData) {
        var dataTable = new google.visualization.DataTable();
        dataTable.addColumn('string', 'Año');
        dataTable.addColumn('number', 'FOSUNAB');

        if (filteredData.length != 0) {
            var trimestres = [filteredData[0].Year];
            trimestres.forEach(trimestreName => {
                var indicador = filteredData.find(item => item.Year === trimestreName)?.Indicador || 0;
                dataTable.addRow([trimestreName, indicador]);
            });
        }

        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#F49004'],
            chartArea: { width: '70%' }
        };

        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Acciones_Ejecutadas_Areas_Fosunab_Anual'));
        chart.draw(dataTable, options);
    }
    //
    function renderSelectIndicadorAccionesEjecutadasPICOAnual() {
        var areas = [...new Set(data_IndicadorAccionesEjecutadasPICOAnual.map(item => item.PICO))];
        var select = document.getElementById('Indicador_Acciones_Ejecutadas_PICO_Anual_Select');
        select.innerHTML = '<option value="">Seleccione la cultura organizacional</option>';
        areas.forEach(area => {
            var option = document.createElement('option');
            option.value = area;
            option.textContent = area;
            select.appendChild(option);
        });
        $('.selectpicker').selectpicker("refresh");
        select.addEventListener('change', function () {
            var selectedArea = this.value;
            if (selectedArea) {
                renderChartIndicadorAccionesEjecutadasPICOAnual(data_IndicadorAccionesEjecutadasPICOAnual.filter(item => item.PICO === selectedArea));
            }
        });
    }

    function renderChartIndicadorAccionesEjecutadasPICOAnual(filteredData) {
        var dataTable = new google.visualization.DataTable();
        dataTable.addColumn('string', 'Año');
        dataTable.addColumn('number', 'FOSCAL');
        dataTable.addColumn('number', 'FOSUNAB');

        if (filteredData.length != 0) {
            var trimestres = [filteredData[0].Year];

            trimestres.forEach(trimestreName => {
                var dataForTrimestre = filteredData.filter(item => item.Year === trimestreName);

                var totalFoscal = dataForTrimestre.reduce((sum, item) => sum + item.Foscal, 0);
                var totalFosunab = dataForTrimestre.reduce((sum, item) => sum + item.Fosunab, 0);
                dataTable.addRow([trimestreName, totalFoscal, totalFosunab]);
            });
        }

        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#3366CC', '#F49004'],
            chartArea: { width: '70%' }
        };

        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Acciones_Ejecutadas_PICO_Anual'));
        chart.draw(dataTable, options);
    }
    //
    function renderChartIndicadorAccionesEjecutadasPICOsAnual(filteredData) {
        var dataTable = new google.visualization.DataTable();
        dataTable.addColumn('string', 'Año');
        dataTable.addColumn('number', 'FOSCAL');
        dataTable.addColumn('number', 'FOSUNAB');

        if (filteredData.length != 0) {
            var trimestres = [filteredData[0].Year];

            trimestres.forEach(trimestreName => {
                var dataForTrimestre = filteredData.filter(item => item.Year === trimestreName);

                var totalFoscal = dataForTrimestre.reduce((sum, item) => sum + item.Foscal, 0);
                var totalFosunab = dataForTrimestre.reduce((sum, item) => sum + item.Fosunab, 0);
                dataTable.addRow([trimestreName, totalFoscal, totalFosunab]);
            });
        }

        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#3366CC', '#F49004'],
            chartArea: { width: '70%' }
        };

        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Acciones_Ejecutadas_PICOs_Anual'));
        chart.draw(dataTable, options);
    }
    //
    function renderChartIndicadorAccionesEjecutadasDIAnual(filteredData) {
        var dataTable = new google.visualization.DataTable();
        dataTable.addColumn('string', 'Año');
        dataTable.addColumn('number', 'FOSCAL');
        dataTable.addColumn('number', 'FOSUNAB');

        if (filteredData.length != 0) {
            var trimestres = [filteredData[0].Year];

            trimestres.forEach(trimestreName => {
                var dataForTrimestre = filteredData.filter(item => item.Year === trimestreName);

                var totalFoscal = dataForTrimestre.reduce((sum, item) => sum + item.Foscal, 0);
                var totalFosunab = dataForTrimestre.reduce((sum, item) => sum + item.Fosunab, 0);
                dataTable.addRow([trimestreName, totalFoscal, totalFosunab]);
            });
        }

        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#3366CC', '#F49004'],
            chartArea: { width: '70%' }
        };

        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Acciones_Ejecutadas_DI_Anual'));
        chart.draw(dataTable, options);
    }
    //
    function renderChartIndicadorAccionesEjecutadasSUHAnual(filteredData) {
        var dataTable = new google.visualization.DataTable();
        dataTable.addColumn('string', 'Año');
        dataTable.addColumn('number', 'FOSCAL');
        dataTable.addColumn('number', 'FOSUNAB');

        if (filteredData.length != 0) {
            var trimestres = [filteredData[0].Year];

            trimestres.forEach(trimestreName => {
                var dataForTrimestre = filteredData.filter(item => item.Year === trimestreName);

                var totalFoscal = dataForTrimestre.reduce((sum, item) => sum + item.Foscal, 0);
                var totalFosunab = dataForTrimestre.reduce((sum, item) => sum + item.Fosunab, 0);
                dataTable.addRow([trimestreName, totalFoscal, totalFosunab]);
            });
        }

        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#3366CC', '#F49004'],
            chartArea: { width: '70%'}
        };

        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Acciones_Ejecutadas_SUH_Anual'));
        chart.draw(dataTable, options);
    }

    ////////////INDICADORES ASISTENTES TRIMESTRE/////////////////////////
    function renderChartIndicadorAsistentesGeneral(filteredData) {
        var dataTable = new google.visualization.DataTable();
        dataTable.addColumn('string', 'Trimestre');
        dataTable.addColumn('number', 'FOSCAL');
        dataTable.addColumn('number', 'FOSUNAB');

        var trimestres = ['Primer Trimestre', 'Segundo Trimestre', 'Tercer Trimestre', 'Cuarto Trimestre'];

        trimestres.forEach(trimestreName => {
            var dataForTrimestre = filteredData.filter(item => item.Trimestre === trimestreName);

            var totalFoscal = dataForTrimestre.reduce((sum, item) => sum + item.Foscal, 0);
            var totalFosunab = dataForTrimestre.reduce((sum, item) => sum + item.Fosunab, 0);
            dataTable.addRow([trimestreName, totalFoscal, totalFosunab]);
        });

        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#3366CC', '#F49004'],
            chartArea: { width: '70%' }
        };

        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Asistentes_General'));
        chart.draw(dataTable, options);
    }
    //
    function renderSelectIndicadorAsistentesAreaFoscal() {
        var areas = [...new Set(data_IndicadorAsistentesAreaFoscal.map(item => item.Area))];
        var select = document.getElementById('Indicador_Asistentes_Area_Foscal_Select');
        select.innerHTML = '<option value="">Seleccione un Área</option>';
        areas.forEach(area => {
            var option = document.createElement('option');
            option.value = area;
            option.textContent = area;
            select.appendChild(option);
        });
        $('.selectpicker').selectpicker("refresh");
        select.addEventListener('change', function () {
            var selectedArea = this.value;
            if (selectedArea) {
                renderChartIndicadorAsistentesAreaFoscal(data_IndicadorAsistentesAreaFoscal.filter(item => item.Area === selectedArea));
            }
        });
    }

    function renderChartIndicadorAsistentesAreaFoscal(filteredData) {
        var dataTable = new google.visualization.DataTable();
        dataTable.addColumn('string', 'Trimestre');
        dataTable.addColumn('number', 'FOSCAL');

        var trimestres = ['Primer Trimestre', 'Segundo Trimestre', 'Tercer Trimestre', 'Cuarto Trimestre'];
        trimestres.forEach(trimestreName => {
            var indicador = filteredData.find(item => item.Trimestre === trimestreName)?.Indicador || 0;
            dataTable.addRow([trimestreName, indicador]);
        });

        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#3366CC', '#F49004'],
            chartArea: { width: '70%' }
        };

        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Asistentes_Area_Foscal'));
        chart.draw(dataTable, options);
    }
    //
    function renderChartIndicadorAsistentesAreasFoscal(filteredData) {
        var dataTable = new google.visualization.DataTable();
        dataTable.addColumn('string', 'Trimestre');
        dataTable.addColumn('number', 'FOSCAL');

        var trimestres = ['Primer Trimestre', 'Segundo Trimestre', 'Tercer Trimestre', 'Cuarto Trimestre'];
        trimestres.forEach(trimestreName => {
            var indicador = filteredData.find(item => item.Trimestre === trimestreName)?.Indicador || 0;
            dataTable.addRow([trimestreName, indicador]);
        });

        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#3366CC', '#F49004'],
            chartArea: { width: '70%' }
        };

        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Asistentes_Areas_Foscal'));
        chart.draw(dataTable, options);
    }
    //
    function renderSelectIndicadorAsistentesAreaFosunab() {
        var areas = [...new Set(data_IndicadorAsistentesAreaFosunab.map(item => item.Area))];
        var select = document.getElementById('Indicador_Asistentes_Area_Fosunab_Select');
        select.innerHTML = '<option value="">Seleccione un Área</option>';
        areas.forEach(area => {
            var option = document.createElement('option');
            option.value = area;
            option.textContent = area;
            select.appendChild(option);
        });
        $('.selectpicker').selectpicker("refresh");
        select.addEventListener('change', function () {
            var selectedArea = this.value;
            if (selectedArea) {
                renderChartIndicadorAsistentesAreaFosunab(data_IndicadorAsistentesAreaFosunab.filter(item => item.Area === selectedArea));
            }
        });
    }

    function renderChartIndicadorAsistentesAreaFosunab(filteredData) {
        var dataTable = new google.visualization.DataTable();
        dataTable.addColumn('string', 'Trimestre');
        dataTable.addColumn('number', 'FOSUNAB');

        var trimestres = ['Primer Trimestre', 'Segundo Trimestre', 'Tercer Trimestre', 'Cuarto Trimestre'];
        trimestres.forEach(trimestreName => {
            var indicador = filteredData.find(item => item.Trimestre === trimestreName)?.Indicador || 0;
            dataTable.addRow([trimestreName, indicador]);
        });

        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#F49004'],
            chartArea: { width: '70%' }
        };

        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Asistentes_Area_Fosunab'));
        chart.draw(dataTable, options);
    }
    //
    function renderChartIndicadorAsistentesAreasFosunab(filteredData) {
        var dataTable = new google.visualization.DataTable();
        dataTable.addColumn('string', 'Trimestre');
        dataTable.addColumn('number', 'FOSUNAB');

        var trimestres = ['Primer Trimestre', 'Segundo Trimestre', 'Tercer Trimestre', 'Cuarto Trimestre'];
        trimestres.forEach(trimestreName => {
            var indicador = filteredData.find(item => item.Trimestre === trimestreName)?.Indicador || 0;
            dataTable.addRow([trimestreName, indicador]);
        });

        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#F49004'],
            chartArea: { width: '70%' }
        };

        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Asistentes_Areas_Fosunab'));
        chart.draw(dataTable, options);
    }
    //
    function renderSelectIndicadorAsistentesPICO() {
        var areas = [...new Set(data_IndicadorAsistentesPICO.map(item => item.PICO))];
        var select = document.getElementById('Indicador_Asistentes_PICO_Select');
        select.innerHTML = '<option value="">Seleccione la cultura organizacional</option>';
        areas.forEach(area => {
            var option = document.createElement('option');
            option.value = area;
            option.textContent = area;
            select.appendChild(option);
        });
        $('.selectpicker').selectpicker("refresh");
        select.addEventListener('change', function () {
            var selectedArea = this.value;
            if (selectedArea) {
                renderChartIndicadorAsistentesPICO(data_IndicadorAsistentesPICO.filter(item => item.PICO === selectedArea));
            }
        });
    }

    function renderChartIndicadorAsistentesPICO(filteredData) {
        var dataTable = new google.visualization.DataTable();
        dataTable.addColumn('string', 'Trimestre');
        dataTable.addColumn('number', 'FOSCAL');
        dataTable.addColumn('number', 'FOSUNAB');

        var trimestres = ['Primer Trimestre', 'Segundo Trimestre', 'Tercer Trimestre', 'Cuarto Trimestre'];

        trimestres.forEach(trimestreName => {
            var dataForTrimestre = filteredData.filter(item => item.Trimestre === trimestreName);

            var totalFoscal = dataForTrimestre.reduce((sum, item) => sum + item.Foscal, 0);
            var totalFosunab = dataForTrimestre.reduce((sum, item) => sum + item.Fosunab, 0);
            dataTable.addRow([trimestreName, totalFoscal, totalFosunab]);
        });

        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#3366CC', '#F49004'],
            chartArea: { width: '70%' }
        };

        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Asistentes_PICO'));
        chart.draw(dataTable, options);
    }
    //
    function renderChartIndicadorAsistentesPICOs(filteredData) {
        var dataTable = new google.visualization.DataTable();
        dataTable.addColumn('string', 'Trimestre');
        dataTable.addColumn('number', 'FOSCAL');
        dataTable.addColumn('number', 'FOSUNAB');

        var trimestres = ['Primer Trimestre', 'Segundo Trimestre', 'Tercer Trimestre', 'Cuarto Trimestre'];

        trimestres.forEach(trimestreName => {
            var dataForTrimestre = filteredData.filter(item => item.Trimestre === trimestreName);

            var totalFoscal = dataForTrimestre.reduce((sum, item) => sum + item.Foscal, 0);
            var totalFosunab = dataForTrimestre.reduce((sum, item) => sum + item.Fosunab, 0);
            dataTable.addRow([trimestreName, totalFoscal, totalFosunab]);
        });

        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#3366CC', '#F49004'],
            chartArea: { width: '70%' }
        };

        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Asistentes_PICOs'));
        chart.draw(dataTable, options);
    }
    //
    function renderChartIndicadorAsistentesDI(filteredData) {
        var dataTable = new google.visualization.DataTable();
        dataTable.addColumn('string', 'Trimestre');
        dataTable.addColumn('number', 'FOSCAL');
        dataTable.addColumn('number', 'FOSUNAB');

        var trimestres = ['Primer Trimestre', 'Segundo Trimestre', 'Tercer Trimestre', 'Cuarto Trimestre'];

        trimestres.forEach(trimestreName => {
            var dataForTrimestre = filteredData.filter(item => item.Trimestre === trimestreName);

            var totalFoscal = dataForTrimestre.reduce((sum, item) => sum + item.Foscal, 0);
            var totalFosunab = dataForTrimestre.reduce((sum, item) => sum + item.Fosunab, 0);
            dataTable.addRow([trimestreName, totalFoscal, totalFosunab]);
        });

        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#3366CC', '#F49004'],
            chartArea: { width: '70%' }
        };

        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Asistentes_DI'));
        chart.draw(dataTable, options);
    }
    //
    function renderChartIndicadorAsistentesSUH(filteredData) {
        var dataTable = new google.visualization.DataTable();
        dataTable.addColumn('string', 'Trimestre');
        dataTable.addColumn('number', 'FOSCAL');
        dataTable.addColumn('number', 'FOSUNAB');

        var trimestres = ['Primer Trimestre', 'Segundo Trimestre', 'Tercer Trimestre', 'Cuarto Trimestre'];

        trimestres.forEach(trimestreName => {
            var dataForTrimestre = filteredData.filter(item => item.Trimestre === trimestreName);

            var totalFoscal = dataForTrimestre.reduce((sum, item) => sum + item.Foscal, 0);
            var totalFosunab = dataForTrimestre.reduce((sum, item) => sum + item.Fosunab, 0);
            dataTable.addRow([trimestreName, totalFoscal, totalFosunab]);
        });

        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#3366CC', '#F49004'],
            chartArea: { width: '70%' }
        };

        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Asistentes_SUH'));
        chart.draw(dataTable, options);
    }
    ////////////////// INDICADORES ASISTENTES ANUAL //////////////
    function renderChartIndicadorAsistentesGeneralAnual(Results) {
        var data2 = [['Año', 'FOSCAL', 'FOSUNAB']];
        for (i = 0; i < Results.IndicadorAsistentesGeneralAnual.length; i++) {
            var item = Results.IndicadorAsistentesGeneralAnual[i];
            data2.push([item.Year, item.Foscal, item.Fosunab]);
        }
        var datos = new google.visualization.arrayToDataTable(data2);
        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#3366CC', '#F49004'],
            chartArea: { width: '70%' }
        };
        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Asistentes_General_Anual'));
        chart.draw(datos, options);
    }
    //
    function renderSelectIndicadorAsistentesAreaFoscalAnual() {
        var areas = [...new Set(data_IndicadorAsistentesAreaFoscalAnual.map(item => item.Area))];
        var select = document.getElementById('Indicador_Asistentes_Area_Foscal_Anual_Select');
        select.innerHTML = '<option value="">Seleccione un Área</option>';
        areas.forEach(area => {
            var option = document.createElement('option');
            option.value = area;
            option.textContent = area;
            select.appendChild(option);
        });
        $('.selectpicker').selectpicker("refresh");
        select.addEventListener('change', function () {
            var selectedArea = this.value;
            if (selectedArea) {
                renderChartIndicadorAsistentesAreaFoscalAnual(data_IndicadorAsistentesAreaFoscalAnual.filter(item => item.Area === selectedArea));
            }
        });
    }

    function renderChartIndicadorAsistentesAreaFoscalAnual(filteredData) {
        var dataTable = new google.visualization.DataTable();
        dataTable.addColumn('string', 'Año');
        dataTable.addColumn('number', 'FOSCAL');

        if (filteredData.length != 0) {
            var trimestres = [filteredData[0].Year];
            trimestres.forEach(trimestreName => {
                var indicador = filteredData.find(item => item.Year === trimestreName)?.Indicador || 0;
                dataTable.addRow([trimestreName, indicador]);
            });
        }

        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#3366CC'],
            chartArea: { width: '70%' }
        };

        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Asistentes_Area_Foscal_Anual'));
        chart.draw(dataTable, options);
    }
    //
    function renderChartIndicadorAsistentesAreasFoscalAnual(filteredData) {
        var dataTable = new google.visualization.DataTable();
        dataTable.addColumn('string', 'Año');
        dataTable.addColumn('number', 'FOSCAL');

        if (filteredData.length != 0) {
            var trimestres = [filteredData[0].Year];
            trimestres.forEach(trimestreName => {
                var indicador = filteredData.find(item => item.Year === trimestreName)?.Indicador || 0;
                dataTable.addRow([trimestreName, indicador]);
            });
        }

        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#3366CC'],
            chartArea: { width: '70%' }
        };

        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Asistentes_Areas_Foscal_Anual'));
        chart.draw(dataTable, options);
    }
    //
    function renderSelectIndicadorAsistentesAreaFosunabAnual() {
        var areas = [...new Set(data_IndicadorAsistentesAreaFosunabAnual.map(item => item.Area))];
        var select = document.getElementById('Indicador_Asistentes_Area_Fosunab_Anual_Select');
        select.innerHTML = '<option value="">Seleccione un Área</option>';
        areas.forEach(area => {
            var option = document.createElement('option');
            option.value = area;
            option.textContent = area;
            select.appendChild(option);
        });
        $('.selectpicker').selectpicker("refresh");
        select.addEventListener('change', function () {
            var selectedArea = this.value;
            if (selectedArea) {
                renderChartIndicadorAsistentesAreaFosunabAnual(data_IndicadorAsistentesAreaFosunabAnual.filter(item => item.Area === selectedArea));
            }
        });
    }

    function renderChartIndicadorAsistentesAreaFosunabAnual(filteredData) {
        var dataTable = new google.visualization.DataTable();
        dataTable.addColumn('string', 'Año');
        dataTable.addColumn('number', 'FOSUNAB');

        if (filteredData.length != 0) {
            var trimestres = [filteredData[0].Year];
            trimestres.forEach(trimestreName => {
                var indicador = filteredData.find(item => item.Year === trimestreName)?.Indicador || 0;
                dataTable.addRow([trimestreName, indicador]);
            });
        }

        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#F49004'],
            chartArea: { width: '70%' }
        };

        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Asistentes_Area_Fosunab_Anual'));
        chart.draw(dataTable, options);
    }
    //
    function renderChartIndicadorAsistentesAreasFosunabAnual(filteredData) {
        var dataTable = new google.visualization.DataTable();
        dataTable.addColumn('string', 'Año');
        dataTable.addColumn('number', 'FOSUNAB');

        if (filteredData.length != 0) {
            var trimestres = [filteredData[0].Year];
            trimestres.forEach(trimestreName => {
                var indicador = filteredData.find(item => item.Year === trimestreName)?.Indicador || 0;
                dataTable.addRow([trimestreName, indicador]);
            });
        }

        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#F49004'],
            chartArea: { width: '70%' }
        };

        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Asistentes_Areas_Fosunab_Anual'));
        chart.draw(dataTable, options);
    }
    //
    function renderSelectIndicadorAsistentesPICOAnual() {
        var areas = [...new Set(data_IndicadorAsistentesPICOAnual.map(item => item.PICO))];
        var select = document.getElementById('Indicador_Asistentes_PICO_Anual_Select');
        select.innerHTML = '<option value="">Seleccione la cultura organizacional</option>';
        areas.forEach(area => {
            var option = document.createElement('option');
            option.value = area;
            option.textContent = area;
            select.appendChild(option);
        });
        $('.selectpicker').selectpicker("refresh");
        select.addEventListener('change', function () {
            var selectedArea = this.value;
            if (selectedArea) {
                renderChartIndicadorAsistentesPICOAnual(data_IndicadorAsistentesPICOAnual.filter(item => item.PICO === selectedArea));
            }
        });
    }

    function renderChartIndicadorAsistentesPICOAnual(filteredData) {
        var dataTable = new google.visualization.DataTable();
        dataTable.addColumn('string', 'Año');
        dataTable.addColumn('number', 'FOSCAL');
        dataTable.addColumn('number', 'FOSUNAB');

        if (filteredData.length != 0) {
            var trimestres = [filteredData[0].Year];

            trimestres.forEach(trimestreName => {
                var dataForTrimestre = filteredData.filter(item => item.Year === trimestreName);

                var totalFoscal = dataForTrimestre.reduce((sum, item) => sum + item.Foscal, 0);
                var totalFosunab = dataForTrimestre.reduce((sum, item) => sum + item.Fosunab, 0);
                dataTable.addRow([trimestreName, totalFoscal, totalFosunab]);
            });
        }

        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#3366CC', '#F49004'],
            chartArea: { width: '70%' }
        };

        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Asistentes_PICO_Anual'));
        chart.draw(dataTable, options);
    }
    //
    function renderChartIndicadorAsistentesPICOsAnual(filteredData) {
        var dataTable = new google.visualization.DataTable();
        dataTable.addColumn('string', 'Año');
        dataTable.addColumn('number', 'FOSCAL');
        dataTable.addColumn('number', 'FOSUNAB');

        if (filteredData.length != 0) {
            var trimestres = [filteredData[0].Year];

            trimestres.forEach(trimestreName => {
                var dataForTrimestre = filteredData.filter(item => item.Year === trimestreName);

                var totalFoscal = dataForTrimestre.reduce((sum, item) => sum + item.Foscal, 0);
                var totalFosunab = dataForTrimestre.reduce((sum, item) => sum + item.Fosunab, 0);
                dataTable.addRow([trimestreName, totalFoscal, totalFosunab]);
            });
        }

        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#3366CC', '#F49004'],
            chartArea: { width: '70%' }
        };

        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Asistentes_PICOs_Anual'));
        chart.draw(dataTable, options);
    }
    //
    function renderChartIndicadorAsistentesDIAnual(filteredData) {
        var dataTable = new google.visualization.DataTable();
        dataTable.addColumn('string', 'Año');
        dataTable.addColumn('number', 'FOSCAL');
        dataTable.addColumn('number', 'FOSUNAB');

        if (filteredData.length != 0) {
            var trimestres = [filteredData[0].Year];

            trimestres.forEach(trimestreName => {
                var dataForTrimestre = filteredData.filter(item => item.Year === trimestreName);

                var totalFoscal = dataForTrimestre.reduce((sum, item) => sum + item.Foscal, 0);
                var totalFosunab = dataForTrimestre.reduce((sum, item) => sum + item.Fosunab, 0);
                dataTable.addRow([trimestreName, totalFoscal, totalFosunab]);
            });
        }

        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#3366CC', '#F49004'],
            chartArea: { width: '70%' }
        };

        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Asistentes_DI_Anual'));
        chart.draw(dataTable, options);
    }
    //
    function renderChartIndicadorAsistentesSUHAnual(filteredData) {
        var dataTable = new google.visualization.DataTable();
        dataTable.addColumn('string', 'Año');
        dataTable.addColumn('number', 'FOSCAL');
        dataTable.addColumn('number', 'FOSUNAB');

        if (filteredData.length != 0) {
            var trimestres = [filteredData[0].Year];

            trimestres.forEach(trimestreName => {
                var dataForTrimestre = filteredData.filter(item => item.Year === trimestreName);

                var totalFoscal = dataForTrimestre.reduce((sum, item) => sum + item.Foscal, 0);
                var totalFosunab = dataForTrimestre.reduce((sum, item) => sum + item.Fosunab, 0);
                dataTable.addRow([trimestreName, totalFoscal, totalFosunab]);
            });
        }

        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#3366CC', '#F49004'],
            chartArea: { width: '70%' }
        };

        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Asistentes_SUH_Anual'));
        chart.draw(dataTable, options);
    }

    //////////////////// INDICADORES EDUFOSCAL /////////////////////

    function renderChartIndicadorAccionesEjecutadasGeneralEduFoscal(filteredData) {
        var dataTable = new google.visualization.DataTable();
        dataTable.addColumn('string', 'Trimestre');
        dataTable.addColumn('number', 'FOSCAL');
        dataTable.addColumn('number', 'FOSUNAB');

        var trimestres = ['Primer Trimestre', 'Segundo Trimestre', 'Tercer Trimestre', 'Cuarto Trimestre'];

        trimestres.forEach(trimestreName => {
            var dataForTrimestre = filteredData.filter(item => item.Trimestre === trimestreName);

            var totalFoscal = dataForTrimestre.reduce((sum, item) => sum + item.Foscal, 0);
            var totalFosunab = dataForTrimestre.reduce((sum, item) => sum + item.Fosunab, 0);
            dataTable.addRow([trimestreName, totalFoscal, totalFosunab]);
        });

        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#3366CC', '#F49004'],
            chartArea: { width: '70%' }
        };
        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Acciones_Ejecutadas_General_EduFoscal'));
        chart.draw(dataTable, options);
    }

    function renderChartIndicadorAccionesEjecutadasGeneralEduFoscalAnual(Results) {
        var data2 = [['Año', 'FOSCAL', 'FOSUNAB']];
        for (i = 0; i < Results.IndicadorAccionesEjecutadasGeneralEduFoscalAnual.length; i++) {
            var item = Results.IndicadorAccionesEjecutadasGeneralEduFoscalAnual[i];
            data2.push([item.Year, item.Foscal, item.Fosunab]);
        }
        var datos = new google.visualization.arrayToDataTable(data2);
        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#3366CC', '#F49004'],
            chartArea: { width: '70%' }
        };
        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Acciones_Ejecutadas_General_EduFoscal_Anual'));
        chart.draw(datos, options);
    }

    function renderChartIndicadorAsistentesGeneralEduFoscal(filteredData) {
        var dataTable = new google.visualization.DataTable();
        dataTable.addColumn('string', 'Trimestre');
        dataTable.addColumn('number', 'FOSCAL');
        dataTable.addColumn('number', 'FOSUNAB');

        var trimestres = ['Primer Trimestre', 'Segundo Trimestre', 'Tercer Trimestre', 'Cuarto Trimestre'];

        trimestres.forEach(trimestreName => {
            var dataForTrimestre = filteredData.filter(item => item.Trimestre === trimestreName);

            var totalFoscal = dataForTrimestre.reduce((sum, item) => sum + item.Foscal, 0);
            var totalFosunab = dataForTrimestre.reduce((sum, item) => sum + item.Fosunab, 0);
            dataTable.addRow([trimestreName, totalFoscal, totalFosunab]);
        });

        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#3366CC', '#F49004'],
            chartArea: { width: '70%' }
        };

        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Asistentes_General_EduFoscal'));
        chart.draw(dataTable, options);
    }

    function renderChartIndicadorAsistentesGeneralEduFoscalAnual(Results) {
        var data2 = [['Año', 'FOSCAL', 'FOSUNAB']];
        for (i = 0; i < Results.IndicadorAsistentesGeneralEduFoscalAnual.length; i++) {
            var item = Results.IndicadorAsistentesGeneralEduFoscalAnual[i];
            data2.push([item.Year, item.Foscal, item.Fosunab]);
        }
        var datos = new google.visualization.arrayToDataTable(data2);
        var options = {
            bars: 'vertical',
            width: 800,
            height: 200,
            colors: ['#3366CC', '#F49004'],
            chartArea: { width: '70%' }
        };
        var chart = new google.visualization.ColumnChart(document.getElementById('Indicador_Asistentes_General_EduFoscal_Anual'));
        chart.draw(datos, options);
    }

</script>
