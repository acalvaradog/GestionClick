@page "/reload"
@page "/reload/{url}"
@inject NavigationManager NavigationManager

@code {
    [Parameter]
    public string? url { get; set; }

    protected override void OnInitialized()
    {
        string urlArreglada = url.Replace(".","/");
        NavigationManager.NavigateTo(urlArreglada);
    }
}